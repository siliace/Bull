#include <Bull/Utility/Image/STBSaver.hpp>

namespace Bull
{
    namespace prv
    {
        bool STBSaver::isSupportedFormat(ImageFormat format) const
        {

        }

        bool STBSaver::isSupportedExtension(const String& extension) const
        {

        }

        bool STBSaver::saveToPath(const Image& resource, const Path& path) const
        {

        }

        bool STBSaver::saveToStream(const Image& resource, OutStream& stream) const
        {

        }

        bool STBSaver::saveToMemory(const Image& resource, void* data, Index length) const
        {

        }
    }
}